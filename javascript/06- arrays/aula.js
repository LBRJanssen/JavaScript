/*
// Criar um Array vazio
const jotting = [];

// Adicionar itens ao Array
jotting.push(10);
jotting.push(10);
jotting.push(10);
jotting.push(10);
jotting.push(10);
jotting.push(10);
jotting.push(10);
jotting.push(10);
jotting.push(10);


// Somar as notas
let add = 0;

for (let i = 0; i < jotting.length; i++) {
    add += jotting[i];
}

// Calcular a média das notas

const mediaNotas = add / jotting.length;

// Exibir a média das notas
console.log(`A média das notas é ${mediaNotas.toFixed(2)}.`);



/*  
// Criar um array vazio para armazenar as notas
const notas = [];

// Adicionar as notas ao array
notas.push(7);
notas.push(8);
notas.push(6);
notas.push(9);
notas.push(5);
notas.push(8);
notas.push(7);

// Calcular a soma das notas
let somaNotas = 0;
for (let i = 0; i < notas.length; i++) {
  somaNotas += notas[i];
}

// Calcular a média das notas
const mediaNotas = somaNotas / notas.length;

// Exibir a média das notas
console.log(`A média das notas é ${mediaNotas.toFixed(2)}.`);
*/

